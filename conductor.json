{
  "scripts": {
    "setup": "ln -sf \"$CONDUCTOR_ROOT_PATH/.env\" .env && ln -sf ../../.env packages/web/.env && ln -sf ../../.env packages/engine/.env && bun install",
    "run": "bun run dev:engine & bun run dev:web --port $CONDUCTOR_PORT"
  }
}
